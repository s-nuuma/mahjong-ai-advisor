# リアルタイム理由付き麻雀育成サポートアプリ 実装仕様書

本ドキュメントは、「実践的な麻雀育成AIアプリ」を構築するための機能要件と実装ステップを定義します。

## 1. システム全体構成アーキテクチャ

- **UI/UX (Next.js/React):** 盤面描画、ユーザー入力受付、AIアドバイスの表示。
- **麻雀エンジン (`@kobalab/majiang-core`):** 牌山生成、配牌、ツモ、打牌、役判定、点数計算などの厳密なルール処理を担当。
- **AIブリッジ (Context Construction):** 現在の局面データと、エンジンから計算した「有効牌」「シャンテン数」などを結合し、Gemini用のプロンプトに変換。
- **推論AI (Gemini 3 Flash / Pro & 外部エンジン):** プロンプトから最適な一打と論理的な理由を推論し、必要に応じて専門エンジンの数値を翻訳。

---

## 2. 実装ステップと詳細仕様

現在の実装状況を各ステップに追記しました。

### ステップ1：ゲームエンジン (majiang-core) の統合
現在の「見た目だけの牌」に、麻雀の厳密なルールと論理を流し込みます。

- **[✅ 完了] ライブラリ導入:** `@kobalab/majiang-core` をインストールし、`Game` クラスを初期化。
- **[✅ 完了] ステート管理:** エンジン内のデータ（牌山、各プレイヤーの手牌・河・点数）をReactのステートと完全に同期させる。
- **[✅ 完了] 基本ループ:** ツモ → 打牌 → （鳴き判定） → ツモ... のサイクルを自動進行させる。
- **[✅ 完了] CPUロジック:** エンジン標準の `Player` クラスを使い、自分以外が自動で打牌（ツモ切り含む）するように設定。

### ステップ2：AIブリッジ（データ翻訳とプロンプト設計）
内部のプログラムデータ（1m, 2s等）を、Gemini が思考できる「専門的な文脈」に変換します。

- **[✅ 完了] コンテキスト抽出:** 現在の手牌、河、ドラ、自風・場風、点数状況をJSON化。エンジンから算出した「有効牌の枚数」や「シャンテン数」もあらかじめ付与（AIの計算ミスを防ぐため）。
- **[✅ 完了] Gemini への命令（システムプロンプト）:** 「あなたはプロ麻雀士です。提供されたデータに基づき、期待値最大の一打を理由付きで回答してください」と定義。
- **[✅ 完了] 出力形式の固定:** 出力形式を JSON（`{ "recommendedDiscard": "3s", "reason": "...", ... }`）に固定し、プログラムで扱いやすくする。

### ステップ3：リアルタイム・コーチングUIの実装
AIの思考をユーザーが直感的に理解できるインターフェースを構築します。

- **[✅ 完了] アドバイス窓:** 画面右側のサイドバーに、Geminiからの「理由」や「外部AIの期待値（EV）」をチャット形式で表示。
- **[✅ 完了] 牌のハイライト:** Geminiが推奨した牌に青い外枠（グロー効果）を出し、逆に危険牌には赤い警告を表示。
- **[✅ 完了] 添削モード:** ユーザーが牌をクリックした際、一旦停止してAIの推奨と比較。不一致なら「なぜAIは別を勧めるか」の解説をポップアップ。
- **[✅ 完了] ホバー情報:** 牌にマウスを重ねると、その牌を切った場合の「有効牌（受け入れ）」をツールチップで表示。
- **[✅ 完了] 視覚的改善:** 牌の表示をテキストからUnicode麻雀牌へ置き換え、UIレイアウト（親番の可視化、ドラ表示など）を整備して視認性を高める。

### ステップ4：高度な解析と外部エンジン（Mortal等）の統合
アドバイスの精度を「プロレベル」まで引き上げ、復習機能を充実させます。

- **[✅ モック完了 / ⏳ 将来対応] 専門エンジンの連携:** アーキテクチャとして「外部AIのEV計算モック（`fetchExternalAnalysis`）」を組み込み済み。UI上にはプログレスバーでEV値を可視化し、Geminiにはその意図を翻訳させるプロンプトへ改修完了。実際のMortal等のバックエンドサーバーへのAPI通信の差し替えは今後の拡張タスク（別の機会）とする。
- **[✅ 完了] 局後レビュー機能:** 一局が終わった後、その局の全ログを Gemini に渡し、「この5巡目が勝負の分かれ目でした」といった詳細なフィードバックを生成。
- **[✅ 完了] 成長グラフ:** AIの推奨打牌とユーザーの打牌の一致率を記録し、上達度を視覚化（グラフ描画）する。
