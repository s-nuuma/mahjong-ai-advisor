# リアルタイム理由付き麻雀育成サポートアプリ 今後の実装仕様書

本ドキュメントは、現在のモックUI基盤から「実践的な麻雀育成AIアプリ」へと昇華させるための、今後の機能要件と実装ステップを定義します。

## 1. システム全体構成アーキテクチャ

- **UI/UX (Next.js/React):** 盤面描画、ユーザー入力受付、AIアドバイスの表示。
- **麻雀エンジン (`@kobalab/majiang-core`):** 牌山生成、配牌、ツモ、打牌、役判定、点数計算などの厳密なルール処理を担当。
- **AIブリッジ (Context Construction):** 現在の局面データと、エンジンから計算した「有効牌」「シャンテン数」などを結合し、Gemini用のプロンプトに変換。
- **推論AI (Gemini 2.5 Flash):** 与えられたプロンプトから最適な一打と論理的な理由を高速で推論。

---

## 2. 実装フェーズと詳細仕様

### フェーズ 1: 麻雀エンジン (`majiang-core`) の組み込み

現在のハードコードされたモックデータを廃止し、実際のゲームループを稼働させます。

- **ゲーム初期化処理:**
  - 対局の開始（東1局から）と、各プレイヤーへの配牌生成。
  - CPUプレイヤーの簡易ロジックの実装（ツモ切り、または簡易な牌効率ベースの打牌）。
- **プレイヤーターン処理:**
  - 自分がツモを行った段階でUIが更新され、打牌操作を受け付ける。
  - 手牌のソート機能（理牌）の実装。
- **状態管理 (State Management):**
  - `gameState` を React の状態として保持し、ターンが進むごとに `majiang-core` の最新の状態（手牌、河、ドラなど）を UI と同期させる。

### フェーズ 2: コンテキスト・ブリッジの高度化（AIへの情報付与）

Geminiにただ盤面を渡すだけでなく、プロ専用の「解析データ」を付与して回答精度を劇的に高めます。

- **シャンテン数計算:**
  - `majiang-core` を用いて現在の手牌のシャンテン数を毎ターン計算。
- **有効牌（受け入れ枚数）の計算:**
  - どの牌を切れば、次に向かうべき有効牌の枚数が最大になるかをエンジン側で事前にリストアップ。
- **プロンプトへの注入:**
  - _「現在のシャンテン数は1です。打1mの場合の有効牌はX枚、打9sの場合はY枚です」_ といった客観的数値をプロンプトに含め、Geminiが「期待値」に基づいた理由付けを行えるようにする。

### フェーズ 3: インテリジェントUIの拡張

「育成・指導」を目的とした機能を追加し、ユーザーの学習効率を高めます。

- **添削モード（自己評価機能）:**
  - AIにアドバイスを求める前に、ユーザーが自力で牌を切る。
  - 切った牌と、AIの推奨牌が異なっていた場合にのみ「ちょっと待って！」というストップが入り、解説が表示される仕組み。
- **動的ハイライトと予測表示:**
  - 手牌にマウスホバーした際、「この牌を切った場合の有効牌一覧」をポップアップ表示する機能。
- **鳴き（副露）への対応:**
  - ポン、チー、カンの選択ダイアログの実装。
  - 鳴くべきかどうかの「鳴き判断アドバイス」をGeminiから取得する機能の実装。

### フェーズ 4: 対局の振り返りとデータ保存

局が終了した後に、学習の成果を確認できるようにします。

- **局ごとのスコア・履歴保存:**
  - Firebase や LocalStorage を使用して対局のログ（打牌の一致率など）を保存。
- **局後レビュー (Gemini Pro):**
  - 局が終了した際、その局での最も重要な選択（分岐点）をGemini Proで深く解析し、「あの時のこの選択が勝負を分けました」という長文のフィードバックを提供する。

---

## 3. 次のステップでの具体的なタスク

1. `src/lib/mahjong-engine.ts` （または専用のクラス）を作成し、`majiang-core` の初期化とゲーム進行ロジックをカプセル化する。
2. `page.tsx` の `initialGameState` を廃止し、エンジンの初期化メソッドから最初の配牌情報を取得してレンダリングするように改修。
3. 手牌のクリックイベントに、実際の打牌処理（エンジン側への伝達とターン進行）を割り当てる。
