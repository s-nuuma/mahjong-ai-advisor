# グローバル開発規約 (GEMINI.md)

本リポジトリで動作するすべてのAIエージェントおよび開発者は、本規約を厳守して開発を進めてください。

---

## 1. 基本行動規範
- **コミットメッセージの厳守**: Conventional Commits 規約を必ず適用してください。
  - フォーマット: `<type>(<scope>): <subject>`
  - type例: `feat`, `fix`, `docs`, `style`, `refactor`, `test`, `chore`
- **セキュリティの担保**: APIキーや秘密鍵、サービスアカウントファイル（`.env`, `*.json`, `*.pem`, `*.key`）をコード内に直接記述（ハードコード）することを厳禁とします。必ず環境変数ファイル（`.env.local` 等）を使用し、`.env.example` を最新に維持してください。
- **最優先参照**: 技術スタックの選択や実装方法については、必ず `tech_stack_rules.md` を最優先で参照・準拠してください。

---

## 2. 開発プロセス（TDD & 視覚的検証）
開発におけるハルシネーションの防止と品質担保のため、以下のプロセスを徹底します。

1. **仕様定義 (`spec.md`)**:
   - 新機能の実装や改修を行う際は、まず `spec.md` に要件と期待される動作を明記し、人間と合意を形成します。
2. **テストファースト (TDD)**:
   - 実装コードを書き換える前に、必ず仕様に基づくテストコード（Vitestでのユニットテスト、またはPlaywrightでのE2Eテスト）を記述・更新します。
   - 「このテストがパスするようにコードを実装・修正する」アプローチを厳格に守ってください。
3. **視覚的フィードバックと自己解決**:
   - UIの変更を伴う場合は、内蔵ブラウザ（Manager Surface）でアプリを起動して実際の画面を確認し、動作結果のスクリーンショットや動画を必ず記録に残してください。
   - 表示崩れやデザインの不一致がある場合、その画像への人間からのコメント（視覚的フィードバック）を即座にコードへ反映してください。

---

## 3. コスト意識と効率設計
- **Firestoreの最適化**:
  - Cloud Firestore の読み書き回数を最小限に抑える効率的なクエリおよびスキーマ設計を行ってください。不要なドキュメントの再取得を避けるキャッシュ制御や構造化を意識してください。

---

## 4. Windows / WSL (Ubuntu) 混在環境における解決ガイドライン
本プロジェクトは、ホストOS（Windows）と実行環境（WSL/Ubuntu）が混在するクロスプラットフォーム環境で開発されます。エージェントが操作する際の誤作動を防ぐため、以下を徹底してください。

- **改行コードの固定**:
  - 改行コードは `LF` に固定します（`.gitattributes` および `.prettierrc` で制御）。自動変換による不要なGit差分を発生させないでください。
- **相対パスの原則使用**:
  - ファイル操作やツール呼び出し時のパス指定には、Windows側の絶対パス（UNCパス `\\wsl.localhost\...`）やWSL側の絶対パス（`/home/asyun/...`）を極力避け、**プロジェクトルートからの相対パス**を使用してください。
- **パッケージマネージャーの一本化**:
  - パッケージのインストールやスクリプト実行には `npm` を使用してください。`pnpm-lock.yaml` や `yarn.lock` を混在させず、`package-lock.json` のみを管理対象とします。
